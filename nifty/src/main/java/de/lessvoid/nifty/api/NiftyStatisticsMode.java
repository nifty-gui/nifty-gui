package de.lessvoid.nifty.api;

/**
 * The different modes of displaying statistics for rendering. You can change the statistics display mode with a call
 * to @see {@link nifty.showStatistics(NiftyStatisticsMode)}
 */
public enum NiftyStatisticsMode {
  ShowFPS
}
